# AirportData

Published BTS aviation data — aggregated airport-level parquets plus a handful of airport
reference and pricing tables. The heavy raw survey extracts are deliberately not here; this
repo carries only the small derived outputs that dashboards and forecast models actually read.

**Read [`METHODOLOGY.md`](./METHODOLOGY.md) first for anything analytical.** It is the
authoritative reference for sample fractions, the DB1B-to-OD40 survey break, fare and yield
contracts, carrier-field semantics, and the change history. This README does not repeat any of
it. This README is the orientation layer: what each file and directory is, who writes it, who
reads it, and on what schedule.

**This is a PUBLIC repo and it must stay public.** Its primary consumer is a browser-based
dashboard that reads these parquets directly with DuckDB-WASM over anonymous
`https://raw.githubusercontent.com/.../master/<path>` URLs — no server, no credentials, no
build step in between. Two consequences follow, and both are load-bearing:

1. **Do not make this repo private.** Every raw URL would start returning 404 and the airport
   dashboard would break instantly.
2. **Never enable Git LFS here.** `raw.githubusercontent.com` serves the LFS *pointer stub*
   (a ~130-byte text file) instead of the real object, so DuckDB-WASM and `pandas.read_parquet`
   would both fail on it. There is no `.gitattributes` in this repo today; keep it that way.
   Files too large for plain git belong in Dropbox, not in LFS — see the size rule in
   [`.gitignore`](./.gitignore).

## Datasets

### Directory-based BTS series

Every directory holds two file shapes per period. **The `_aggregated` suffix means
airport-level rollup**; the file without it is the wider, un-rolled-up table.

| Path | What it is | Source | Grain / coverage | Regenerated by | Cadence |
|---|---|---|---|---|---|
| `db1b_mkt/dbmkt_YYYY.parquet` | Directional origin-destination market table with fare and mileage components | BTS Origin & Destination Survey — DB1B Market (`db1b_download.py:50`, `transtats.bts.gov/PREZIP/Origin_and_Destination_Survey_DB1BMarket_{y}_{q}.zip`); 2025 Q3+ rows are derived from OD40/DB1C instead | Origin x Dest x Year x Quarter; 1993–2025 | `db1b_download.py:733,803-804` (legacy) / `od40_derive.py:648` (current merge) | Monthly via OD40 pipeline |
| `db1b_mkt/dbmkt_YYYY_aggregated.parquet` | Dashboard-facing per-origin rollup of the above; carrier stripped | same | Origin x Period (quarter); 1993–2026 | `db1b_download.py:738,808-809` / `od40_derive.py:648` | Monthly |
| `db1b_tix/dbtix_YYYY.parquet` | Ticket/itinerary economics by origin: round-trip vs one-way pax, gross and net fare dollars, pax-miles | BTS DB1B Ticket (`db1b_download.py:51`); 2025 Q3+ derived from OD40 | Origin x Year x Quarter; 1993–2025 | `db1b_download.py:868-869` / `od40_derive.py:858` | Monthly |
| `db1b_tix/dbtix_YYYY_aggregated.parquet` | Dashboard-facing per-origin ticket rollup | same | Origin x Period (quarter); 1993–2026 | `db1b_download.py:876-877` / `od40_derive.py:858` | Monthly |
| `t100_seg/t100_seg_YYYY.parquet` | Raw T-100 segment records: departures scheduled/performed, seats, passengers, payload, freight, mail, air time, by carrier | BTS T-100 Segment (All Carriers), form code `FMG` at `transtats.bts.gov/DL_SelectFields.aspx` (`t100_download.py:63`) | Carrier x Origin x Dest x Month; 1990–2026 | `t100_download.py:313` | Monthly (~15th) |
| `t100_seg/t100_seg_YYYY_aggregated.parquet` | Airport-quarter rollup: EP, DP, PAX, departures, landings, operations, payload | same | Airport x Year x Quarter; 1990–2026 | `t100_aggregate.py:395` | Monthly |
| `t100_mkt/t100_mkt_YYYY.parquet` | Raw T-100 market records: passengers, freight, mail, distance, by carrier | BTS T-100 Market (All Carriers), form code `FMF` (`t100_download.py:62`) | Carrier x Origin x Dest x Month; 1990–2026 | `t100_download.py:273` | Monthly (~15th) |
| `t100_mkt/t100_mkt_YYYY_aggregated.parquet` | Airport-quarter rollup: EP, DP, PAX, freight in/out, mail in/out, outbound passenger-miles | same | Airport x Year x Quarter; 1990–2026 | `t100_aggregate.py:395` | Monthly |
| `od40/mkt/od40_mkt_YYYY.parquet` | OD40 market table, source-native monthly grain | BTS OD40 / DB1C monthly survey, Azure blob `ostrapeispubdownloadprod.blob.core.windows.net/.../db1c_public/DB1C.PUBLIC.YYYYMM.RELnn.<date>.zip`; URLs discovered at runtime from the BTS O&D page (`od40_download.py:61-65`, fallback map at `:65-113`) | Origin x Dest x Year x Month; 2025–2026 | `od40_derive.py:873-884` | Monthly (~10th) |
| `od40/mkt/od40_mkt_YYYY_aggregated.parquet` | Per-origin quarterly rollup | same | Origin x Year x Quarter | `od40_derive.py:726` | Monthly |
| `od40/tix/od40_tix_YYYY{,_aggregated}.parquet` | OD40 ticket economics by origin (gross comparable and net tax-exclusive components) | same | Origin x Month / Origin x Quarter; 2025–2026 | `od40_derive.py:774,873-884` | Monthly |
| `od40/carrier/od40_car_YYYY.parquet` | Per-carrier tickets, PAX, RPM under three attribution bases | same | Carrier x Attribution x Year x Month; 2025–2026 | `od40_derive.py:873-884` | Monthly |
| `od40/{mkt,tix,carrier}/month/od40_*_YYYY_MM.parquet` | Single-month slices of the three OD40 tables | same | as above, one month per file; 2025-07 – 2026-03 | `od40_derive.py:571-578,927` | Monthly |
| `od40/comparability-20260417.json` | One-off DB1B vs OD40 comparability report used to characterise the survey break | derived from both surveys | snapshot, dated 2026-04-17 | `compare_db1b_od40.py:794` (run on demand, not scheduled) | Ad hoc |
| `form41/p12/form41_p12_YYYY_Qq.parquet` | System operating expenses by objective (salaries, benefits, fuel, maintenance, …) | BTS Form 41 Schedule P-6, form code `FME` (`form41_download.py:65-68`); note the local name `p12` maps to BTS's P-6 table (`form41_download.py:9`) | Carrier x Year x Quarter; 2019 Q1 – 2026 Q1 | `form41_download.py:391` | Monthly (~20th) |
| `form41/p52/form41_p52_YYYY_Qq.parquet` | Operating expenses by aircraft group | BTS Form 41 Schedule P-5.2, form code `FMK` (`form41_download.py:70-73`, `:10`) | Carrier x Aircraft group x Year x Quarter; 2019 Q1 – 2026 Q1 | `form41_download.py:391` | Monthly (~20th) |
| `form41/p6/form41_p6_YYYY_Qq.parquet` | Traffic and capacity by aircraft type (ASMs, block hours) | BTS Form 41 Schedule T-2, form code `FIH` (`form41_download.py:75-78`); local name `p6` maps to BTS's T-2 table (`form41_download.py:11`) | Carrier x Aircraft type x Year x Quarter; 2019 Q1 – 2026 Q1 | `form41_download.py:391` | Monthly (~20th) |

The `db1b_*` and `t100_*` directories cover their full historical span; only the current year's
`_aggregated` file changes on a routine refresh. `db1b_mkt`/`db1b_tix` have `_aggregated` files
through 2026 but un-aggregated files only through 2025.

### Loose top-level files

| File | What it is | Source | Grain / coverage | Written by | Cadence |
|---|---|---|---|---|---|
| `airport_fuel.parquet` | 100LL and Jet A retail prices by airport, FBO, fuel type, and service type | AirNav.com airport pages, scraped; every row carries a `Source_URL`. See METHODOLOGY.md's "Airport fuel prices" section | Weekly snapshot x Airport x FBO x Fuel x Service; 2,719 rows; `snapshot_date` is the authoritative freshness field | `util/fuel-price` — `run.py:226-230` does `git add`/`commit`/`push origin master` on a local checkout | Weekly (`launchd/com.airport-fuel.weekly.plist`) |
| `Airport_CFC.parquet` | Rental-car Customer Facility Charge per airport, with rate basis and scrape/publish dates | RentalCar.com feed, scraped and audited by `util/rental-car-cfc` | Airport; 223 rows; one current rate per airport | `util/rental-car-cfc/src/publish.py:298` builds it into Dropbox — **the push to this repo is a manual `gh api -X PUT`, not automated** (see Caveats) | Ad hoc / manual |
| `NPIAS_Airports.parquet` | FAA airport classifications: service level, hub size, role, CY23 enplanements, based aircraft, 5-year development estimate | FAA National Plan of Integrated Airport Systems (NPIAS). The column set maps 1:1 onto the FY25 NPIAS Appendix A workbook (`ARP-NPIAS-2025-2029-AppendixA.xlsx`, sheet "All NPIAS Airports"), which is read by `util/tsa-throughput/code/Rscript_AirportLevel_HubTrends_UnisonPackage.R:493-509` | Airport (LocID); 3,287 rows | **No generator found.** Manually built and uploaded | Manual; last classification refresh 2026-02-10 per METHODOLOGY.md |
| `ntad_airports.parquet` | Airport point geometry and hub size: lat/lon, facility type/name, state, county | National Transportation Atlas Database (NTAD) — described as "NTAD" in `util/fuel-price/src/airports.py:3`; the specific NTAD layer and vintage are **`[SOURCE UNVERIFIED]`** | Airport (Code); 3,265 rows | **No generator found.** Manually built and uploaded | Manual |
| `L_CITY_MARKET_ID.parquet` | BTS city-market ID to city-name lookup | BTS lookup table (the `L_*` prefix is BTS's own naming convention for TranStats lookup tables); download step is **`[SOURCE UNVERIFIED]`** | Code x Description; 5,999 rows | **No generator found.** Read-only in `db1b_download.py:63-64` | Manual |
| `Report127_Compiled.parquet` | Airport operating revenue and expense lines by fiscal year: aeronautical / non-aeronautical revenue, personnel, utilities, depreciation, etc. | **`[SOURCE UNVERIFIED]`.** The line items match FAA airport financial reporting (Form 127); a separate dashboard, `dash/cats`, is described as a "Form 127 Analytics Dashboard" (`dash/cats/CLAUDE.md:5`) but it loads its own bundled JSON, not this file | State x Hub Size x LocID x FYE; 5,027 rows | **No generator found** | Static |
| `air_to_truck.parquet` | Air-to-truck intermodal freight facility points: lat/lon, airport code, operator names | **`[SOURCE UNVERIFIED]`** — presumably the parquet form of the CSV below | Facility; 404 rows | **No generator found** | Static |
| `Intermodal_Freight_Facilities_Air-to-Truck.csv` | CSV counterpart of the above | **`[SOURCE UNVERIFIED]`** — the name matches an NTAD intermodal freight facilities layer, but nothing in code confirms it | Facility | **No generator found.** Tracked despite the `*.csv` rule in `.gitignore`, so it was force-added | Static |
| `data_availability.json` | Machine-readable per-dataset period coverage (`min_period`/`max_period` plus the full quarter list); the dashboard reads it to decide which year files to request | derived by scanning this repo's own parquets | one object per dataset | `util/dot-download/update_availability.py:195`, called from `od40_pipeline.py:19,93`, `od40_derive.py:950-951`, `db1b_download.py:967-968` | Every OD40 refresh |
| `data_manifest.json` | Download provenance log: dataset, year, quarter, `downloaded_at`, row count, file path | derived at download time | one record per downloaded file; 283 records | `db1b_download.py:201`, `t100_download.py:203`, `od40_download.py:317`, `form41_download.py:296` | Every refresh |

## Consumers

| Consumer | Repo / file | Datasets used | Fetch type |
|---|---|---|---|
| Airport dashboard | `dash/airport` — `src/lib/duckdb.ts:67-108` (URL builders), `src/hooks/useData.ts` (queries), `src/utils/api.ts:1` | `db1b_mkt` (aggregated + OD), `db1b_tix` (aggregated), `t100_mkt`/`t100_seg` (aggregated), `od40/mkt`, `ntad_airports`, `NPIAS_Airports`, `Airport_CFC`, `L_CITY_MARKET_ID`, `airport_fuel`, `data_availability.json`. **Does not read `form41`.** | **Browser.** DuckDB-WASM in a Web Worker plus plain `fetch()` for the JSON |
| Airport dashboard fuel page | `dash/airport/src/pages/AirportFuel.tsx:374` | `airport_fuel.parquet` | **Browser.** A user-facing "Raw parquet" download link straight to the raw URL |
| Fuel-price pipeline | `util/fuel-price` — `config.py:19-20`, `src/airports.py:181-200` | `NPIAS_Airports`, `ntad_airports` (to build its target airport list) | **Server-side.** `httpx` download to a local cache, then `pandas.read_parquet` |
| Rental-car CFC pipeline | `util/rental-car-cfc` — `config.py:19`, `src/airports.py:39`, `src/publish.py:35-37,256-258` | `NPIAS_Airports`, `ntad_airports`, and its own prior `Airport_CFC.parquet` (for the name lookup) | **Server-side.** `urllib.request.urlopen` + `pandas.read_parquet` |
| BTS pipeline itself | `util/dot-download` — `db1b_download.py:63-64,70-71`, `od40_derive.py:103-104` | `L_CITY_MARKET_ID`, `NPIAS_Airports` (read as reference inputs during aggregation) | **Server-side, local checkout** |
| Route profitability model | `unison/route-profit` — `config.py:21-22`, `process_t100.py:47`, `process_db1b.py:131`, `process_db1b_coupon.py:145,558-559`, `process_od40.py:48` | `form41`, `t100_seg`, `db1b_mkt/quarter`, `db1b_tix/quarter`, `od40/month` | **Server-side, local Dropbox checkout** (uses raw quarterly/monthly files that are *not* in this repo — see Caveats) |
| Traffic forecast engagements | `unison/traffic-forecasts` — `forecasts/{ewr,bhm,mke,orf,stl}-2026/config.py`, `forecasts/mry-{2025,2026}/config.py` and `scripts/build_db1b_inputs.py:54-55` / `:65-66`, `forecasts/stl-2026/src/db1b_connecting_backcast.py:17` | `t100_seg`, `t100_mkt`, `db1b_mkt` (aggregated and `quarter/`), `db1b_tix` (aggregated), `od40/mkt` | **Server-side, local Dropbox checkout** |
| Route dashboard | `dash/route` | none directly — it renders JSON bundles that `unison/route-profit/build_app_data.py` derives from the local checkout | Indirect |

`form41` has no dashboard consumer; its only reader is the route profitability model.
`data_manifest.json` has **no reader anywhere** — four scripts write it and nothing consumes it.

## How the data is refreshed

The producing pipeline is **`dot-download`** (local path `~/Projects/util/dot-download`; remote
`github.com/garycl/dot-download`, which is **private**). Read `dot-download/RUNBOOK.md` before
running anything by hand.

**Automated path — GitHub Actions `bts-refresh.yml`**
(`dot-download/.github/workflows/bts-refresh.yml`). The workflow checks out this repo with
`secrets.AIRPORTDATA_PUSH_TOKEN`, runs the pipeline with `--data-dir` pointed at that checkout
and `--no-push`, then does its own `git add -A` / commit / push:

| Cron (UTC) | Dataset | BTS publication window |
|---|---|---|
| `0 12 10 * *` | OD40 — `od40_pipeline.py` | ~10th of the month |
| `0 12 15 * *` | T-100 — `t100_download.py` | ~15th of the month |
| `0 12 20 * *` | Form 41 — `form41_download.py` | ~20th of the month |

The "Pick dataset by date" step accepts the scheduled day or one day late and fails loudly on
any other date rather than guessing. `workflow_dispatch` takes a `dataset` input
(`od40` / `t100` / `form41`) to force a specific run.

`airport_fuel.parquet` is on a separate weekly schedule owned by `util/fuel-price`
(`launchd/com.airport-fuel.weekly.plist`), which pushes directly to this repo.

**Legacy path — launchd on the maintainer's Mac.** `com.bts-data.od40.plist` (day 10, 06:00),
`com.bts-data.t100.plist` (day 15, 06:00), `com.bts-data.form41.plist` (day 20, 06:00). There
is deliberately **no** DB1B plist — see the survey-break caveat below.

**Manual run**, from a `dot-download` checkout with this repo cloned alongside:

```
python3 od40_pipeline.py   --no-push --data-dir ../AirportData   # current O&D
python3 t100_download.py   --no-push --data-dir ../AirportData
python3 form41_download.py --no-push --data-dir ../AirportData

# then, from this repo's checkout:
git add -A
git commit -m "data: <dataset> refresh <YYYY-MM> (manual fallback)"
git push
```

Verify by checking that `data_availability.json` advanced to the period you expected. Note that
`t100_download.py` and `form41_download.py` do **not** refresh `data_availability.json` — for
those, confirm the new parquet files under the dataset directory instead.

## Caveats

### METHODOLOGY.md is the authoritative analytical reference

Do not do fare, yield, passenger, or carrier analysis off this README.
[`METHODOLOGY.md`](./METHODOLOGY.md) carries the binding rules: sample fractions and reweighting
multipliers, the additive fare/yield component contracts (never average precomputed fares or
yields), which carrier field pairs with which across datasets, the NPIAS filtering order, and
the changelog including the 2026-04 `AvgMktFare` pax-weighting fix that invalidated every
pre-2026-04-17 downstream extract.

### The 2025 Q2 to Q3 survey break is real

BTS retired the quarterly DB1B survey after 2025 Q2 and replaced it with the monthly OD40
(DB1C) survey — a different sample fraction (10% vs 40%), different reporting rules, different
carrier coverage, different time grain. The DB1B-shaped files in `db1b_mkt/` and `db1b_tix/`
**blend both surveys across 2025** (Q1/Q2 legacy DB1B, Q3/Q4 OD40 rescaled to 10%-equivalent)
so dashboards see a continuous series. The break is still there. METHODOLOGY.md section
"Important: methodology break at 2025 Q2 to Q3" is required reading before any cross-2025
comparison.

Operationally this means: **do not run `db1b_download.py` for a current refresh.** It is
disabled by default and requires `--allow-legacy-db1b`. A pure DB1B rebuild can overwrite the
merged 2025 Q3+ OD40 rows and silently regress `data_availability.json` and every dashboard
back to 2025 Q2. After any bounded legacy backfill, rerun `od40_pipeline.py --no-push` and
require the merge validation to pass before committing.

### Raw per-period extracts are excluded by size

The raw DB1B per-quarter and OD40 per-month parquets are 100 MB to 1 GB each and are not in
this repo — see the size-based rules in `.gitignore` (`db1b_mkt/dbmkt_*_[1-4].parquet`,
`db1b_mkt/quarter/`, `db1b_tix/quarter/`, `od40/month/`, `*.csv`). They live only in the
maintainer's local Dropbox data directory.

This matters because **several consumers read those excluded files, not this repo**:
`unison/route-profit` reads `db1b_mkt/quarter/`, `db1b_tix/quarter/` and `od40/month/`, and
`traffic-forecasts/forecasts/stl-2026` and `mry-{2025,2026}` read `db1b_mkt/quarter/`. A
successor who clones this repo will *not* be able to run those projects. They need either the
Dropbox data directory or a re-download via `dot-download`.

### This repo was re-imported on 2026-08-05

History was intentionally not carried over. `git log` shows a single commit,
"Initial import: current aggregates as of 2026-08-05". Treat this repo as a current-state
snapshot: pre-2026-08-05 provenance lives in METHODOLOGY.md's changelog, not in git history.
There is no earlier commit to diff against or revert to.

### Publish target vs. this repo

The GitHub Action targets `Unison-Consulting-Inc/AirportData` (`bts-refresh.yml:17`), but every
piece of *runtime* code still fetches `garycl/AirportData`:

- `dash/airport/src/lib/duckdb.ts:68` and `src/utils/api.ts:1` (the latter is hardcoded and
  ignores the `VITE_DATA_URL` override)
- `util/fuel-price/config.py:19-20`
- `util/rental-car-cfc/config.py:19`, `src/publish.py:35-37`

`garycl/AirportData` and `Unison-Consulting-Inc/AirportData` are two independent non-fork
repos. Until the consumer base URLs are repointed here, this repo is the *write* target and
`garycl/AirportData` is still the *read* target. Also note
`util/rental-car-cfc/tests/test_imports.py:16` asserts the literal string `garycl/AirportData`
and will fail the moment the URL is migrated.

### `Airport_CFC.parquet` publishing is manual and undocumented in code

`util/rental-car-cfc/src/publish.py` builds the parquet into `$DROPBOX_ROOT/data/rental-car/`
and stops there — it says so explicitly at `src/publish.py:7-9`. Getting it into this repo
requires a human running a `gh api -X PUT ...` command that exists only in prose
(`rental-car-cfc/RUNBOOK.md:46`, `CLAUDE.md:136`), followed by manually bumping
`DATA_FILE_VERSION` in `dash/airport/src/lib/duckdb.ts:71` to bust the raw-URL cache. This is
the most fragile link in the chain.

Note also that the two runbooks contradict each other on how to handle this repo:
`dot-download/RUNBOOK.md:105` instructs you to `git clone` it, while
`rental-car-cfc/RUNBOOK.md:46` says **never** clone it (it is multi-gigabyte). Both are
defensible for their own workflow; be aware of the clone cost before following the former.

### Six files have no generator anywhere — preserve them

`NPIAS_Airports.parquet`, `ntad_airports.parquet`, `L_CITY_MARKET_ID.parquet`,
`Report127_Compiled.parquet`, `air_to_truck.parquet`, and
`Intermodal_Freight_Facilities_Air-to-Truck.csv` are not produced by any script in
`~/Projects`. Nothing can rebuild them.

The first three are actively depended on — `NPIAS_Airports.parquet` alone is read by the
dashboard, the fuel-price pipeline, the CFC pipeline, and the BTS pipeline itself. Losing it
breaks four projects at once. Keep backups outside git.

The last three (`Report127_Compiled.parquet`, `air_to_truck.parquet`, and the intermodal CSV)
have **zero readers and zero writers** anywhere in `~/Projects` — they are orphaned. Do not
delete them on that basis alone; an unrecoverable file with no known consumer is still
unrecoverable, and the consumer may simply be outside this codebase.

### The pipeline code is in a private personal repo

`dot-download`, `fuel-price`, and `rental-car-cfc` are all **private** repos under `garycl`,
not under the org. Org membership alone does not grant access. METHODOLOGY.md's link to
`github.com/garycl/dot-download` will 404 for a successor. Those repos need to be transferred
or forked into the org before anyone else can refresh this data.

### Two schedule gaps worth knowing

- `t100_download.py` and `form41_download.py` do not refresh `data_availability.json`; only the
  OD40 and DB1B paths call `update_availability.py`. A green T-100 run therefore will not move
  `generated_at`.
- `data_manifest.json` is written by four scripts and read by none.
